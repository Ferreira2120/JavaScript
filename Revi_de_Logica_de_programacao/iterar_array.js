const nomes = ["Pedro", "Amanda", "Raimundo", "Sintia"];
console.log(nomes.length);
//como  fazer para mostrar todos os nomes individualmente?

for (let i = 0; i < nomes.length; i++) {
  console.log(`${i}: Nome é ${nomes[i]}`);
}
